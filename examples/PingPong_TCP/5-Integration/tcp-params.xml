<?xml version="1.0" encoding="UTF-8"?>
<!--
  TCP transport parameters for link: link_Ping_to_Pong
  Protocol: TCP (ELI over TCP)
  Referenced by cs1.logical-system.xml transportBinding/@parameters

  Note: ECOA does not standardise the TCP parameters file format.
  This schema is implementation-defined (middleware / LDP vendor).
-->
<TCPBinding xmlns="http://www.ecoa.technology/tcpbinding">

  <!-- Platform_Ping side: listens on this address for incoming ELI connections -->
  <platform name="Platform_Ping"
            address="192.168.1.1"
            port="30000"/>

  <!-- Platform_Pong side: listens on this address for incoming ELI connections -->
  <platform name="Platform_Pong"
            address="192.168.1.2"
            port="30001"/>

</TCPBinding>
